{
  "name": "moveBuild",
  "version": "1.0.0",
  "description": "will delete stuff from dst/ and fill it with stuff from src/",
  "main": "moveBuild.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node moveBuild ../mapa/build ../mapa-deploy/build"
  },
  "keywords": [],
  "author": "endziu",
  "license": "MIT",
  "dependencies": {
    "data.task": "^3.1.1",
    "fs-extra": "^1.0.0",
    "minimist": "^1.2.0"
  }
}
